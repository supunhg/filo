format: msgpack
version: "1.0"
mime:
  - application/msgpack
category: serialization
extensions:
  - msgpack
  - mpk
confidence_weight: 0.3
signatures: []
description: >
  MessagePack format. Efficient binary serialization format that represents
  JSON-like data structures in a compact binary form.
  Note: MessagePack has no fixed magic bytes; detection relies on structural analysis.
references:
  - https://msgpack.org/
